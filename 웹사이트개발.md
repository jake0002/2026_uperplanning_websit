# 프로젝트 개요: 슈퍼플래닝 신규 웹사이트 풀스택 개발 및 AWS 배포

당신은 최고 수준의 시니어 풀스택 개발자이자 UX 디자이너입니다. 아래의 요구사항을 엄격하게 준수하여 Next.js 기반의 웹사이트를 완벽하게 구축하고 배포해 주세요.

## 1. 기술 스택 및 렌더링 환경
* 프레임워크: Next.js 14 (App Router 방식), React
* 스타일링: Tailwind CSS, shadcn/ui
* 데이터베이스: MySQL, Prisma ORM
* 렌더링: SEO 및 GEO 최적화를 위한 Server Component (SSR/SSG) 우선 적용

## 2. UI/UX 디자인 가이드라인 (uiuxjobsboard 스타일)
* **벤치마크:** `uiuxjobsboard.com`과 같이 여백(Negative Space)이 풍부하고 심플하며 감각적인 미니멀 에이전시 스타일을 구현합니다.
* **디자인 시스템:** `shadcn/ui`의 컴포넌트를 활용하되, 불필요한 테두리(Border)를 제거하고 화이트 및 라이트 그레이 톤의 배경과 무채색 계열의 굵은 폰트를 사용하여 텍스트의 시각적 위계를 명확히 합니다.
* **GNB 구조:** 최상단 메인 메뉴는 [홈 | UX프로젝트 | 블로그 | 문의] 4가지만으로 깔끔하게 구성합니다.
* **서브 탭 고정(Sticky):** 'UX프로젝트' 진입 시 화면 최상단에 스크롤을 내려도 고정되는 서브 탭 네비게이션을 만듭니다. 서브 탭 항목은 [UX리서치, UX라이팅, UX기획, UX강의, 웹/앱개발]로 구성하며, 현재 위치(Active)를 진한 포인트 컬러나 밑줄로 직관적으로 표시합니다.

## 3. 데이터베이스 및 스키마 설계 (schema.prisma)
MySQL 데이터베이스를 사용하며 다음 세 가지 주요 모델을 구축합니다.
* **PageMeta 모델:** 각 페이지의 URL별로 Title, Description, GEO_Target_Keyword 등 SEO/GEO 메타 데이터를 자유롭게 저장하고 불러옵니다.
* **Post 모델:** 블로그 포스팅용 테이블 (제목, 내용, 작성일, 카테고리, 썸네일 이미지 등).
* **Inquiry 모델:** 고객 문의 사항 테이블. 필드는 다음과 같이 구성합니다.
  - 문의구분 (Enum: UX리서치, UX라이팅, UX기획, UI디자인, UX강의, 웹/앱개발, 제휴/기타)
  - 이름 (String)
  - 연락처 (String)
  - 이메일 (String)
  - 문의내용 (Text)
  - 등록일시 (DateTime, 기본값 now())

## 4. 어드민(Admin) 대시보드 구축 (`/admin` 경로)
사용자 화면과 분리된 `/admin` 라우트 그룹에 다음 기능을 포함하는 직관적인 대시보드를 만듭니다.
* **SEO/GEO 관리:** 사이트 내 모든 페이지의 메타 태그(제목, 설명, 키워드)를 입력하고 수정(CRUD)할 수 있는 인터페이스.
* **블로그 관리:** 블로그 글을 작성, 조회, 수정, 삭제(CRUD)할 수 있는 에디터 기능.
* **문의 사항 관리:** 사용자가 폼을 통해 제출한 Inquiry 데이터를 최신 등록일시 순으로 조회할 수 있는 리스트 뷰 기능.

## 5. SEO 최적화 및 301 리다이렉트
* 모든 페이지의 `<head>` 영역에 동적 메타 태그와 Schema.org(JSON-LD) 구조화 데이터가 삽입되도록 `layout.tsx`를 구성합니다.
* 동적 `sitemap.xml`과 `robots.txt`를 자동 생성하며, `/admin` 경로는 크롤링을 완전히 차단(Disallow)합니다.
* `next.config.js`에 기존 위블리 URL 영구 이동(301 Redirect)을 설정합니다.
  - `/contact.html` -> `/contact`
  - `/uxplan.html` -> `/services/ux-plan`
  - `/writer.html` -> `/services/ux-writing`
  - `/class.html` -> `/services/ux-class`

## 6. Git 버전 관리 및 보안 (필수)
* 프로젝트 생성 즉시 `git init`을 실행하여 버전 관리를 시작합니다.
* 작업을 서버에 올리기 전 항상 버전을 커밋하며, 모든 커밋 메시지는 반드시 명확하고 이해하기 쉬운 **한글**로 작성합니다 (예: `feat: 고객 문의사항 데이터베이스 모델 추가`).
* `.gitignore` 파일을 생성하고 보안을 위해 `.env` 파일, `.pem` 파일, 데이터베이스 비밀번호, API 키 등이 Git 저장소에 절대 올라가지 않도록 강력하게 차단합니다.

## 7. AWS 배포 환경 설정
AWS Lightsail 환경에 웹사이트를 배포하기 위한 스크립트 또는 설정을 구성합니다.
* 배포 타겟 IP: `13.125.12.112` (사용자: `admin`)
* 인증키 사용: 제공된 `shkey.pem` 활용.
* 배포 경로 매핑: 프론트엔드 서비스는 `superplanning.blog/website/` 경로로, 어드민 시스템은 `superplanning.blog/website/admin` 경로로 배포 및 구동되도록 Nginx 또는 라우팅 환경을 세팅합니다.